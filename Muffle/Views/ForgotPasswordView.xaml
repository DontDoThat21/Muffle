<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Muffle.ViewModels"
             x:Class="Muffle.Views.ForgotPasswordView"
             x:DataType="viewmodels:ForgotPasswordViewModel"
             BackgroundColor="#303030">

    <Grid RowDefinitions="Auto,*" Padding="20" RowSpacing="15">

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Spacing="6">
            <Label Text="Reset Password"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="White"/>
            <Label Text="Enter your account email to receive a password reset code."
                   FontSize="14"
                   TextColor="LightGray"/>
        </VerticalStackLayout>

        <!-- Scrollable content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="15">

                <!-- Status / feedback message -->
                <Label Text="{Binding StatusMessage}"
                       IsVisible="{Binding HasStatusMessage}"
                       HorizontalOptions="Center"
                       TextColor="Orange"
                       FontSize="14"
                       FontAttributes="Bold"/>

                <ActivityIndicator IsVisible="{Binding IsProcessing}"
                                   IsRunning="{Binding IsProcessing}"
                                   Color="White"
                                   HorizontalOptions="Center"/>

                <!-- ── STEP 1: Email entry ── -->
                <Frame IsVisible="{Binding IsStepOne}"
                       Padding="15"
                       BorderColor="#424242"
                       BackgroundColor="#252525"
                       CornerRadius="8"
                       HasShadow="False">
                    <VerticalStackLayout Spacing="14">

                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="4" VerticalOptions="Center">
                                <Label Text="Step 1 — Enter Your Email"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="White"/>
                                <Label Text="We'll send a 6-digit reset code to your registered email address."
                                       FontSize="13"
                                       TextColor="LightGray"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="1"
                                   Text="🔐"
                                   FontSize="28"
                                   VerticalOptions="Center"/>
                        </Grid>

                        <Label Text="Email Address"
                               FontSize="12"
                               TextColor="Gray"/>

                        <Frame CornerRadius="5" Padding="0" BackgroundColor="#40444b" HasShadow="False">
                            <Entry Placeholder="Enter your account email"
                                   PlaceholderColor="#72767d"
                                   TextColor="White"
                                   BackgroundColor="Transparent"
                                   Keyboard="Email"
                                   Text="{Binding Email}"/>
                        </Frame>

                        <Button Text="Send Reset Code"
                                Command="{Binding SendResetCodeCommand}"
                                BackgroundColor="#7289DA"
                                TextColor="White"
                                CornerRadius="6"/>

                        <Label HorizontalOptions="Center">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Back to "/>
                                    <Span Text="Login"
                                          TextColor="#00b0f4">
                                        <Span.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding BackToLoginCommand}"/>
                                        </Span.GestureRecognizers>
                                    </Span>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>

                    </VerticalStackLayout>
                </Frame>

                <!-- ── STEP 2: Code + new password ── -->
                <Frame IsVisible="{Binding IsStepTwo}"
                       Padding="15"
                       BorderColor="#7289DA"
                       BackgroundColor="#252525"
                       CornerRadius="8"
                       HasShadow="False">
                    <VerticalStackLayout Spacing="14">

                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="4" VerticalOptions="Center">
                                <Label Text="Step 2 — Enter Code &amp; New Password"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="White"/>
                                <Label Text="Check your email for the reset code, then choose a new password."
                                       FontSize="13"
                                       TextColor="LightGray"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="1"
                                   Text="✉️"
                                   FontSize="28"
                                   VerticalOptions="Center"/>
                        </Grid>

                        <!-- Simulated email code (dev helper) -->
                        <Frame IsVisible="{Binding HasSimulatedCode}"
                               Padding="12"
                               BackgroundColor="#1E1E1E"
                               CornerRadius="6"
                               HasShadow="False"
                               BorderColor="#555">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="📧 Simulated Email Code (dev preview)"
                                       FontSize="11"
                                       TextColor="Gray"/>
                                <Label Text="{Binding SimulatedCode}"
                                       FontSize="22"
                                       FontAttributes="Bold"
                                       TextColor="#43B581"
                                       HorizontalOptions="Center"
                                       LetterSpacing="4"/>
                                <Label Text="This code expires in 15 minutes."
                                       FontSize="11"
                                       TextColor="Gray"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Label Text="Reset Code"
                               FontSize="12"
                               TextColor="Gray"/>

                        <Frame CornerRadius="5" Padding="0" BackgroundColor="#40444b" HasShadow="False">
                            <Entry Placeholder="6-digit code"
                                   PlaceholderColor="#72767d"
                                   TextColor="White"
                                   BackgroundColor="Transparent"
                                   Keyboard="Numeric"
                                   MaxLength="6"
                                   Text="{Binding VerificationCode}"/>
                        </Frame>

                        <Label Text="New Password"
                               FontSize="12"
                               TextColor="Gray"
                               Margin="0,8,0,0"/>

                        <Frame CornerRadius="5" Padding="0" BackgroundColor="#40444b" HasShadow="False">
                            <Entry Placeholder="New password (8+ characters)"
                                   PlaceholderColor="#72767d"
                                   TextColor="White"
                                   BackgroundColor="Transparent"
                                   IsPassword="True"
                                   Text="{Binding NewPassword}"/>
                        </Frame>

                        <Label Text="Confirm New Password"
                               FontSize="12"
                               TextColor="Gray"/>

                        <Frame CornerRadius="5" Padding="0" BackgroundColor="#40444b" HasShadow="False">
                            <Entry Placeholder="Repeat new password"
                                   PlaceholderColor="#72767d"
                                   TextColor="White"
                                   BackgroundColor="Transparent"
                                   IsPassword="True"
                                   Text="{Binding ConfirmPassword}"/>
                        </Frame>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Button Grid.Column="0"
                                    Text="Reset Password"
                                    Command="{Binding ResetPasswordCommand}"
                                    BackgroundColor="#43B581"
                                    TextColor="White"
                                    CornerRadius="6"/>
                            <Button Grid.Column="1"
                                    Text="Start Over"
                                    Command="{Binding StartOverCommand}"
                                    BackgroundColor="#72767d"
                                    TextColor="White"
                                    CornerRadius="6"/>
                        </Grid>

                    </VerticalStackLayout>
                </Frame>

                <!-- ── COMPLETE ── -->
                <Frame IsVisible="{Binding IsComplete}"
                       Padding="15"
                       BorderColor="#43B581"
                       BackgroundColor="#252525"
                       CornerRadius="8"
                       HasShadow="False">
                    <VerticalStackLayout Spacing="14" HorizontalOptions="Center">

                        <Label Text="✅"
                               FontSize="48"
                               HorizontalOptions="Center"/>

                        <Label Text="Password Reset Successfully"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="White"
                               HorizontalOptions="Center"/>

                        <Label Text="Your password has been updated. You can now log in with your new password."
                               FontSize="13"
                               TextColor="LightGray"
                               HorizontalTextAlignment="Center"/>

                        <Button Text="Back to Login"
                                Command="{Binding BackToLoginCommand}"
                                BackgroundColor="#7289DA"
                                TextColor="White"
                                CornerRadius="6"
                                HorizontalOptions="Center"/>

                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentView>
